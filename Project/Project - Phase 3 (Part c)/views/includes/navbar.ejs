<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid px-4">

    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-end gap-1" href="/listings">
      <i class="bx bx-home-heart"></i>
      <span>Stayly</span>
    </a>

    <!-- ── Search bar (center) ── -->
    <form class="nav-search-form d-none d-md-flex" action="/listings" method="GET">
      <div class="nav-search-wrap">

        <div class="nav-search-field border-end">
          <label>Where</label>
          <input type="text" name="location" placeholder="Destinations" />
        </div>

        <div class="nav-search-field border-end">
          <label>Check in</label>
          <input type="date" name="checkin" />
        </div>

        <div class="nav-search-field border-end">
          <label>Check out</label>
          <input type="date" name="checkout" />
        </div>

        <div class="nav-search-field">
          <label>Guests</label>
          <input type="number" name="guests" placeholder="Add guests" min="1" />
        </div>

        <button type="submit" class="nav-search-btn" aria-label="Search">
          <i class="bi bi-search"></i>
        </button>

      </div>
    </form>

    <!-- Mobile search icon (shows on small screens) -->
    <button class="nav-mobile-search d-flex d-md-none" type="button" data-bs-toggle="collapse"
      data-bs-target="#mobileSearch" aria-label="Open search">
      <i class="bi bi-search"></i>
    </button>

    <!-- Toggler -->
    <button class="navbar-toggler ms-2" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings">Explore</a>
        <a class="nav-link" href="/listings/new">Host a Stay</a>
        <% if (!currentUser) { %>
          <a class="nav-link" href="/Signup">Sign up</a>
          <a class="nav-link" href="/Signin">Sign in</a>
          <% } %>
            <% if (currentUser) { %>
              <a class="nav-link" href="/Signout">Sign out</a>
              <% } %>
      </div>
    </div>

  </div>

  <!-- Mobile search dropdown -->
  <div class="collapse" id="mobileSearch">
    <form class="mobile-search-form" action="/listings" method="GET">
      <div class="mobile-search-field">
        <label>Where</label>
        <input type="text" name="location" placeholder="Search destinations" />
      </div>
      <div class="mobile-search-row">
        <div class="mobile-search-field">
          <label>Check in</label>
          <input type="date" name="checkin" />
        </div>
        <div class="mobile-search-field">
          <label>Check out</label>
          <input type="date" name="checkout" />
        </div>
      </div>
      <div class="mobile-search-field">
        <label>Guests</label>
        <input type="number" name="guests" placeholder="Add guests" min="1" />
      </div>
      <button type="submit" class="mobile-search-submit">
        <i class="bi bi-search me-2"></i>Search
      </button>
    </form>
  </div>

</nav>